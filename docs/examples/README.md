# Example assets

`sample-repair-order.png` — stitched preview of a demo repair-order PDF.

Shop name / logo in the image are **illustrative branding only** (how a configured `shop_name` and `logo_path` show up). They are not documentation of a specific employer or business relationship. Demo customer and vehicle identifiers are fictional.
