# Example assets

`sample-repair-order.png` — stitched preview of a demo repair-order PDF.

Shop name in the image is **illustrative branding only**. The top-right **YOUR LOGO HERE** square marks where a configured `logo_path` appears on a finished PDF — not a real shop mark. Demo customer and vehicle identifiers are fictional.
